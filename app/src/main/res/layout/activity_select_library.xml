<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<RelativeLayout
    android:background="@color/white"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.select_library.SelectLibraryActivity">

    <include
        layout="@layout/page_title_bar"
        android:id="@+id/titleBar"/>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/librarySelectCompleteButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="24dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="24dp"
        android:background="@drawable/gradient_selector_button"
        android:enabled="false"
        android:fontFamily="@font/nanum_extrabold"
        android:text="선택 완료"
        android:textColor="@color/white" />


    <LinearLayout
        android:visibility="gone"
        android:id="@+id/noLibraryImageView"
        android:gravity="center"
        android:orientation="vertical"
        android:layout_centerInParent="true"
        android:layout_width="match_parent"
        android:layout_height="350dp">

        <ImageView
            android:src="@drawable/ic_album_track"
            android:layout_gravity="center"
            android:layout_width="150dp"
            android:layout_height="150dp"
            app:tint="@color/gray" />

        <TextView
            android:textSize="18sp"
            android:layout_marginTop="24dp"
            android:fontFamily="@font/nanum_regular"
            android:text="아직 커버가 존재하지 않습니다!"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <Button
            android:id="@+id/recordButton"
            android:text="녹음하러 가기"
            android:fontFamily="@font/nanum_extrabold"
            android:textColor="@color/white"
            android:layout_marginHorizontal="24dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:background="@drawable/background_gradient_button"/>

    </LinearLayout>

    <com.chauthai.overscroll.RecyclerViewBouncy
        android:id="@+id/libraryList"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/titleBar"
        android:layout_marginTop="48dp"
        app:friction="200"
        app:gapLimit="220"
        app:speedFactor="5"
        app:tension="1000" />

</RelativeLayout>


</layout>